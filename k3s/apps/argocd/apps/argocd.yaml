apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argocd
spec:
  project: default
  # syncPolicy:
  #   automated:
  #     prune: true
  source:
    repoURL: 'git@github.com:lucassiauve/tmp_infra.git'
    targetRevision: HEAD
    path: k3s/apps/argocd
  destination:
    name: in-cluster
    namespace: argocd
