apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: bbox-tls
  namespace: bbox
spec:
  secretName: bbox-tls-secret
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - bbox.armleth.fr
