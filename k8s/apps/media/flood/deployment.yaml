---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: flood
  namespace: media
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: flood
  template:
    metadata:
      labels:
        app: flood
    spec:
      containers:
        - name: flood
          image: jesec/flood:4.13.0
          # --noauth: Authentik handles authentication
          # --qbuser/--qbpass: placeholders required by Flood's CLI parser
          #   even when qBittorrent auth is bypassed (https://github.com/jesec/flood/discussions/100)
          args:
            - --allowedpath
            - /downloads
            - --noauth
            - --qburl
            - http://qbittorrent-service:8080
            - --qbuser
            - placeholder
            - --qbpass
            - placeholder
          env:
            - name: HOME
              value: "/config"
          ports:
            - containerPort: 3000
              name: http
          resources:
            requests:
              memory: 64Mi
              cpu: 50m
            limits:
              memory: 256Mi
          volumeMounts:
            - name: config
              mountPath: /config
            - name: downloads
              mountPath: /downloads
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 20
            periodSeconds: 30
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: flood-config
        - name: downloads
          persistentVolumeClaim:
            claimName: media-downloads
